{
  "name": "mistria-museum-tracker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5174 --strictPort",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 5174 --strictPort",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "vite": "^7.1.7",
    "@playwright/test": "^1.55.1"
  }
}
